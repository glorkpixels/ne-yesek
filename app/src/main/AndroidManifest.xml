<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.deu.neyesek">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NeYesek">
        <activity
            android:name=".Activity.LoginActivity"
            android:theme="@style/Theme.NeYesek.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".Activity.UserDrawer"
            android:label="@string/title_activity_user_drawer"
            android:theme="@style/Theme.NeYesek.NoActionBar"></activity>
        <activity
            android:name=".Activity.RegisterActivity"
            android:theme="@style/Theme.NeYesek.NoActionBar"></activity>
    </application>

</manifest>